# Examples

Reference plugins, one directory each. Copy a directory to start your
own; the contract is plain HTTP and JSON, so any language works.

- [`simple-edit`](simple-edit) - rotate and crop. Pairs, registers a relay button and an open-mode page, and does all
  its work through monbooru's REST API.